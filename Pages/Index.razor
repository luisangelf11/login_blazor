@page "/"
@inject IJSRuntime js
<PageTitle>Index</PageTitle>
<AuthorizeView>
    <Authorized>
        <h1>Hello @context.User.Identity.Name.ToString()</h1>
    </Authorized>
    <NotAuthorized>
        <h1>Hello Guest!</h1>
    </NotAuthorized>
</AuthorizeView>
<h1>Hello, world!</h1>

Welcome to your new app.

<SurveyPrompt Title="How is Blazor working for you?" />

@*<AuthorizeView>
    <Authorized>
        <br /><br />
        <button class="btn btn-primary" @onclick="DisplayGrettingAlert">Display Gretting Alert</button>
    </Authorized>
</AuthorizeView>

@code{
    [CascadingParameter]
    private Task<AuthenticationState> authenticationState { get; set; }
    private async Task DisplayGrettingAlert()
    {
        var authState = authenticationState;
        var message = $"Hello Bro";
        await js.InvokeVoidAsync("alert", message);
    }
}*@